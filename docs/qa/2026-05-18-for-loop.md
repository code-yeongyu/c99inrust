# For Loop QA Evidence

Date: 2026-05-18
Verified compiler commit: `b86b6a0b8653dfdfccfa08ab234756680db4a54b`
Session: `c99-for-qa`
Raw capture: `/tmp/c99inrust-tmux-for-qa.txt`

## Manual tmux QA

The tmux session ran under `bash --noprofile --norc`, compiled a C99 `for`
loop with a scoped `int` initializer, assembled the `c99inrust` output with
`cc`, ran it, then compiled and ran the same source with the host C compiler.

Captured pass lines:

```text
compile_status=0
assemble_status=0
c99_status=10
host_compile_status=0
host_status=10
manual_for_loop=ok
```

The session was closed with `exit`. `tmux kill-server` was not used.
